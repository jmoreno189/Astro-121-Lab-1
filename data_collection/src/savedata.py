"""
Author: Joseph Moreno
Description: Python module designed to save data to the ..\measurements folder. Files and directories are labeled with appropriate metadata unique to the collected data.
"""

import os

# Save data to a local file within a subdirectory of the measurements folder. data should be a Python object; metadata should be a Python dictionary; dir should be a String.
def save_file(data=None, metadata=None, dir=None):
    assert data is not None, "Save attempt failed: missing data."
    assert metadata is not None, "Save attempt failed: missing metadata."

def 

    
    
    
